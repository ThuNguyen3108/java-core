package org.example;

public class bai11_namnhuan {
    public static void main(String[] args) {
        /*
        Bài tập java 06: Viết chương trình nhập vào 1 năm dương lịch, kiểm tra năm đó có phải l năm nhuận hay không
        Gợi ý
        Năm nhuận là năm
        chia hết cho 4 và không chia hết cho 100 hoặc chia hết cho 400
        => (nam % 4 == 0) && (nam % 100 != 0) || (nam % 400 == 0 )

        * */
    }
}
